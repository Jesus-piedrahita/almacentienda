/**
 * @fileoverview Vitest global setup.
 * Importa los matchers de jest-dom para que estén disponibles
 * en todos los tests sin tener que importarlos manualmente.
 */
import '@testing-library/jest-dom';
